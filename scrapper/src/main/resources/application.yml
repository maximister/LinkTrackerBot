app:
  scheduler:
    enable: true
    interval: 5s
    force-check-delay: 5s

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

springdoc:
  swagger-ui:
    path: /swagger-ui
